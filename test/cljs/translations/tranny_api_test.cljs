(ns translations.tranny-api-test
  (:require [clojure.test :refer [deftest testing is async]]
            [translations.tranny-api :refer [lookup-key lookup-key-lang]]))

#_(deftest lookup-key-lang-test
  (testing "lookup-key and language in API"
    (async done))
  )
